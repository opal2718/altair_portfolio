/** @type {import('next').NextConfig} */
//const nextConfig = {};
const nextConfig = {
    basePath: "/altair_portfolio",
    output: "export",  // <=== enables static exports
    reactStrictMode: true,
  };
export default nextConfig;
